services:
  web:
    image: nginx:alpine
    ports:
      - "8080:80"
    environment:
      NGINX_HOST: localhost
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
