{
  "name": "@genie/app",
  "version": "1.0.0",
  "private": true,
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && electron .",
    "build": "tsc",
    "dev": "npm run build && electron ."
  },
  "dependencies": {
    "electron": "^33.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "concurrently": "^9.2.1",
    "electron-reload": "^2.0.0-alpha.1",
    "typescript": "^5.7.0"
  }
}
